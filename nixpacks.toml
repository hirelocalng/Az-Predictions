[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["cd frontend && npm install && npm run build"]

[start]
cmd = "gunicorn app:app"
